import SearchInput from "./components/SearchInput";

const Panel = props => {
    return (
        <nav>
            <SearchInput/>
            {items.count}
        </nav>
    );
}
 export default Panel;